node_modules
node/**/!(main).js
node/**/*.js.map
.vscode/
.DS_Store
node/package-lock.json

# managed-by: golden-path v1
# spec-kit: SpecKit artifacts live at the parent `is-io-specs/.specify/` (multi-repo).
# This repo intentionally does not host `.specify/` locally.
.claude/
.specify/
